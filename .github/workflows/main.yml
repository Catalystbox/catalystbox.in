name: Supabase Dashboard Keep-Alive

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -s "https://evjdmbyncuotprkyhipf.supabase.co/health" --max-time 10
